#include "livetimes.cpp"

void runLivetimes(){

  // for(Int_t i=2484;i<3102;i++)livetimes(i);
  //  for(Int_t i=2996;i<3102;i++)livetimes(i);
  for(Int_t i=3059;i<3227;i++)livetimes(i);
  //  for(Int_t i=2484;i<3227;i++)livetimes(i);
  //  for(Int_t i=1565;i<1862;i++)livetimes(i);
  //  for(Int_t i=2073;i<2097;i++)livetimes(i);
  return;
}
